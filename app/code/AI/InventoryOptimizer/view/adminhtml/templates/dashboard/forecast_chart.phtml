<?php
/**
 * AI Inventory Forecast Chart Template
 */
?>
<div class="dashboard-card">
    <div class="card-header">
        <h2><?= $block->escapeHtml(__('AI Demand Forecast')) ?></h2>
    </div>
    <div class="card-content">
        <canvas id="forecast-chart" width="400" height="300"></canvas>
    </div>
</div>

<script>
require([
    'jquery',
    'chartjs'
], function ($, Chart) {
    'use strict';
    
    $(document).ready(function () {
        initForecastChart();
    });
    
    function initForecastChart() {
        var ctx = document.getElementById('forecast-chart').getContext('2d');
        
        // Sample data - in production this would come from the AI forecasting model
        var forecastData = {
            labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
            datasets: [{
                label: 'Historical Sales',
                data: [12, 19, 3, 5, 2, 3],
                backgroundColor: 'rgba(54, 162, 235, 0.2)',
                borderColor: 'rgba(54, 162, 235, 1)',
                borderWidth: 1
            }, {
                label: 'AI Forecast',
                data: [15, 22, 6, 9, 4, 6],
                backgroundColor: 'rgba(255, 99, 132, 0.2)',
                borderColor: 'rgba(255, 99, 132, 1)',
                borderWidth: 1,
                borderDash: [5, 5]
            }]
        };
        
        var forecastChart = new Chart(ctx, {
            type: 'line',
            data: forecastData,
            options: {
                responsive: true,
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        });
    }
});
</script> 